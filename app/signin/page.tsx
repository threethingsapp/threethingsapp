import { Layout } from "../components/Layout";

type Props = {};

const SignIn = ({}: Props): JSX.Element => {
  return (
    <Layout>
      <h2>Sign In</h2>
    </Layout>
  );
};

export default SignIn;
